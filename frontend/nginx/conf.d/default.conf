server {
    listen 80;
    server_name myportfolioapp.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name myportfolioapp.com;

    ssl_certificate /etc/letsencrypt/live/myportfolioapp.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/myportfolioapp.com/privkey.pem;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ =404;
    }
}
